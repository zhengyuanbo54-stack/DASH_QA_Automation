# DASH QA Automation Code Challenge

## 项目信息
- **完成耗时**：约 2 小时
- **环境**：Windows 11 + PyCharm + Python 3.12
- **核心技术栈**：Appium + Python + pytest + Page Object Model (POM)
- ## 环境说明
- 测试用例因缺少Appium服务、Android模拟器环境，暂无法实际运行，但代码语法、结构、逻辑均符合规范
- 核心功能（POM架构、安全点击、跨平台定位、订单提取）均已通过本地语法+用例结构验证

## 环境依赖
请在终端执行以下命令安装依赖包：
```bash
pip install -r requirements.txt
# 或者直接安装
pip install appium-python-client pytest pyyaml
